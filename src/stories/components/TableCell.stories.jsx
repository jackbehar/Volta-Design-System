import React from 'react';
import { TableCell } from '../../index';
import { getMuiDefaultArgs } from '../muiDefaultArgs';

export default { title: 'Components/TableCell', component: TableCell, tags: ['autodocs'] };

export const Primary = { args: getMuiDefaultArgs('TableCell') };
