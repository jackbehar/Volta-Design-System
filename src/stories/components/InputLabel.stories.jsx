import React from 'react';
import { InputLabel } from '../../index';
import { getMuiDefaultArgs } from '../muiDefaultArgs';

export default { title: 'Components/InputLabel', component: InputLabel, tags: ['autodocs'] };

export const Primary = { args: getMuiDefaultArgs('InputLabel') };
