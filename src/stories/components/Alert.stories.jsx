import React from 'react';
import { Alert } from '../../index';
import { getMuiDefaultArgs } from '../muiDefaultArgs';

export default { title: 'Components/Alert', component: Alert, tags: ['autodocs'] };

export const Primary = { args: getMuiDefaultArgs('Alert') };
