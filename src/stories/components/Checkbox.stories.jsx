import React from 'react';
import { Checkbox } from '../../index';
import { getMuiDefaultArgs } from '../muiDefaultArgs';

export default { title: 'Components/Checkbox', component: Checkbox, tags: ['autodocs'] };

export const Primary = { args: getMuiDefaultArgs('Checkbox') };
