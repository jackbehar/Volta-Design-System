import React from 'react';
import { TextField } from '../../index';
import { getMuiDefaultArgs } from '../muiDefaultArgs';

export default { title: 'Components/TextField', component: TextField, tags: ['autodocs'] };

export const Primary = { args: getMuiDefaultArgs('TextField') };
