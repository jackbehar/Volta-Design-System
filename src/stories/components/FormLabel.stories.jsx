import React from 'react';
import { FormLabel } from '../../index';
import { getMuiDefaultArgs } from '../muiDefaultArgs';

export default { title: 'Components/FormLabel', component: FormLabel, tags: ['autodocs'] };

export const Primary = { args: getMuiDefaultArgs('FormLabel') };
