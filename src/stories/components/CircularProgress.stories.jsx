import React from 'react';
import { CircularProgress } from '../../index';
import { getMuiDefaultArgs } from '../muiDefaultArgs';

export default { title: 'Components/CircularProgress', component: CircularProgress, tags: ['autodocs'] };

export const Primary = { args: getMuiDefaultArgs('CircularProgress') };
