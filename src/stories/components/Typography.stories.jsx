import React from 'react';
import { Typography } from '../../index';
import { getMuiDefaultArgs } from '../muiDefaultArgs';

export default { title: 'Components/Typography', component: Typography, tags: ['autodocs'] };

export const Primary = { args: getMuiDefaultArgs('Typography') };
