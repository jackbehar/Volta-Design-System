import React from 'react';
import { Chip } from '../../index';
import { getMuiDefaultArgs } from '../muiDefaultArgs';

export default { title: 'Components/Chip', component: Chip, tags: ['autodocs'] };

export const Primary = { args: getMuiDefaultArgs('Chip') };
